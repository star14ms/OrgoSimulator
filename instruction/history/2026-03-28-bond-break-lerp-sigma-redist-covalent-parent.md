# Bond-break lerp, guide-group trace, σ formation redist on CovalentBond parent

Since prior compact (`Ref: pending`): **`CovalentBond.BreakBond`** again drives **VSEPR / 3D redistribution** after ex-bond lobes are placed when `instantRedistributionForDestroyPartner` is false: captures σ-neighbor counts, σ-cleavage-between-partners, and π vs σ line for relax flags; **3D** starts **`CoLerpBondBreakRedistribution`** (smoothstep lerp of nucleus-parented targets, then `ApplyRedistributeTargets`, hybrid refresh, then bond GO destroy / electron sync); **2D** calls **`RedistributeOrbitals`** on both atoms with break-guide and ref axes. **`AtomFunction`** adds **`PeekRedistributeTargetsSameAsRedistributeOrbitals3D`** and the coroutine; guide resolution **tier 5** prefers the passed **`bondBreakGuideLoneOrbitalForTargets`** (ex-bond lone) over scanning bonded list; **`GuideGroupFirstVertexDirectionNucleusLocal`** unifies first VSEPR vertex (internuclear for bond guides, pivot→orbital for ex-bond); **`GetRedistributeTargets3D`** always tries guide-group prefix (not skipped when σ-cleavage VSEPR framing is on); **`GetRedistributeTargets3DVseprTryMatch`** accepts **`redistributionOperationBond`** and prepends guide-group when it succeeds; **`LogGuideGroupTrace`** behind **`DebugLogBondBreakTetraFramework`**; joint hybrid rotation helper guards **`fi >= n`**. **`ElectronOrbitalFunction`**: **`OrbitalBelongsToAtomForSigmaFormationRedist`** / **`SigmaFormationRedistTargetHasSignificantDelta`** so σ/π formation step 2 counts and lerps movers **on nucleus or incident `CovalentBond`**; removes over-filtering redistribute lists to source/target bonding orbitals only.
