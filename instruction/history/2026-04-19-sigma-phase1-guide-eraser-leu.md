# Summary (since last /compact)

Working tree vs `HEAD` (`146f0db`): **EditModeManager** records the deleted neighbor position during eraser chain-end breaks and, after re-selecting the bond-returned σ orbital, rotates **that same** `ElectronOrbitalFunction` so local **+X** points along keeper→deleted when it was anti-parallel—fixing wrong-side edit-mode re-adds without swapping orbitals. **OrbitalRedistribution** fixes **σ phase-1** VSEPR matching: `GetHeaviestGroupOpPrioritized` now returns the group whose orbital **is** `atomOrbitalOp` when present, then **`opPathBest`** (same bond axis as the op) instead of always returning the heaviest row—so guide slot 0 aligns to the forming σ, reducing other bond groups crowding the binding corridor. **SigmaBondFormation** / **OrbitalRedistribution** gained **`[σ-p1-redist]`** / **`[σ-p1-empty]`** `Debug.Log` lines plus **NDJSON** to `.cursor/debug-115e1e.log` for phase-1 triage. **AminoAcidMoleculeLibrary** Leu builds the second γ-methyl from a **different** sp³ lobe (cross product preference + optional `SwapAnchorNonbondLobesForBondDirectionTowardWorld`). Several **`.cursor/rules/*.mdc`** files show **empty `description:`** in the diff—likely accidental; restore YAML descriptions before commit if those edits were not intentional.
