# Update summary (since last /compact)

3D electron redistribution and π-step plumbing: guide-group documentation and multiply-bond cluster handling; predictive VSEPR domain inputs for `TryMatch` (partner resolution, merged σ axes, optional exclusion of fading / disappearing lone lobes); `TryMatchLoneOrbitalsToFreeIdealDirections` tracks free slot indices for trigonal lone mapping and logs permutation-cost NDJSON. π-step pair ordering uses `OrderPairAtomsForRedistributionGuideTier` instead of Z/instance-id only; callers pass π orbitals for predictive lone disappearance; hybrid refresh after π goes through `RefreshSigmaBondOrbitalHybridAlignmentForConnectedMoleculeAfterPiStep`.

Heuristic “misleading” rotations are turned off: `MaybeFlipPiTrigonalCanonicalSlotRotForParentHemisphereContinuity` and tetra azimuth optimization are no-ops; trigonal guide ideal uses pure `AlignFirstDirectionTo(GetIdealLocalDirections(3), guide)`; `MinimizeTargetDirsAzimuthForPermutationCostInPlace` returns after baseline perm-cost logging without twisting targets. `VseprLayout` adds `TrigonalIdealRotatedInPlane` for in-plane trigonal rotation (still available for callers; guide path no longer runs the θ grid).

Debug: new flags and `AppendPermCostInvariantNdjson` to `.cursor/debug-d66405.log`; `ProjectAgentDebugLog` ingest filename/session updated to `d66405`. Housekeeping: removed accidental `Assets/_Recovery` Unity scenes; instruction `electron-redistribution-repulsion-guide-group.md` updated; small `ElectronOrbital` prefab tweak. Untracked `Assets/Editor/` remains outside this diff until added.
