# 2026-03-27 — Remove orbital redistribution on bond break and σ formation

Since last `/compact` (ref still pending from prior summary): **AtomFunction** was heavily reduced from a strip pass: core redistribution entry points are no-ops or stubs, while **shared geometry helpers** and **TryCompute* σ-neighbor relax** APIs were restored so the project compiles and other callers still link. **CovalentBond.BreakBond** no longer runs post-break σ-relax, `RedistributeOrbitals`, `OrientEmpty`, bond→slot correction, or the break animation coroutines—3D ex-bond lobes stay at the captured bond-line pose; only refresh/sync and σ line transform updates remain. **ElectronOrbitalFunction** adds **`SkipSigmaFormationStep2GeometryMoves3D`** (default on): 3D σ formation skips rearrange, redistribute targets, σ-relax, Newman stagger, and the step-2 lerp; the bonding orbital is snapped to the bond target instead. Repo **`tools/`** holds optional strip/restore artifacts (Python strip script, helper snippets).
