# Update summary — 2026-03-22 (3D orbital redistribution, bonds, FG)

## Summary (since last /compact)

3D orbital geometry and edit-mode bonding saw substantial work. **AtomFunction** now gates coplanar→tetrahedral σ-neighbor relax behind `relaxCoplanarSigmaToTetrahedral` (bond break only), adds trigonal-planar relax after π re-forms, retries VSEPR slot counts for hard bond-break cases, and rotates **whole substituents** when σ-relax fragments are disjoint (rings fall back to moving only immediate σ neighbors). **Charge** label can flash red and fade on invalid EN-based bond drag. **CovalentBond.BreakBond** captures bond-line orbital pose before unregister, supports drag-break electron rules, and in perspective runs a coroutine that smoothsteps σ-neighbor moves plus both new lone orbitals from bond pose to slot, then `RedistributeOrbitals` with tetrahedral relax. **ElectronOrbitalFunction** animates σ-neighbor trigonal motion in π step 2, blocks drag-break toward less EN with label flash, always redistributes both endpoints after π. **MoleculeBuilder** uses σ bonds with redistribution, `RedistributeOrbitalsOnConnectedMolecule` for FG builds, optional `[FG-OrbDist]` tracing for nitro/carboxyl. **EditModeManager** improves chain-end erase selection. **AtomQuickAddUI** drops unused HUD serialize fields. Minor **build profile** asset tweak.
