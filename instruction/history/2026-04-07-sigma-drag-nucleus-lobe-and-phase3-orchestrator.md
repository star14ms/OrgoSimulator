# Summary (since last /compact)

**Committed (`fb2014c`):** Orbital-drag σ guide axis for phase-1 placement and prebond is unified (drops the old “always flip +X toward partner” rule where it hurt far-hemisphere 1e–1e cases). **`ElectronOrbitalFunction`** documents σ-start **`SnapToOriginal()`** on the dragged lobe before the three-phase runner; **`ElectronOrbital`** prefab shortens default phase-1 prebond timing. **`SigmaBondFormation`** small cleanup. Prior **`instruction/history/2026-04-07-sigma-drag-guide-axis-and-snap.md`** described an older flip-based story; behavior in tree is now geometry-first.

**Working tree (uncommitted):** **`SigmaLobeUnitDirectionFromAtom`** defines the in-op lobe axis as **normalize(orbital world position − parent nucleus world position)** for all electron counts, with degenerate fallback to local +X. **`NormalizedGuideSigmaOpHeadForDrag`** takes the guide **`AtomFunction`**, uses that axis (no extra internuclear sign flip), and feeds both **`ComputeNonGuideNucleusTargetAlongGuideOpHead`** and **`RunSigmaFormation12PrebondNonGuideHybridOnly`** (non-guide **currentHead** uses the same rule). **`RunSigmaFormation12PostbondGuideHybridOnly`** runs **`RunElectronRedistributionForBondEvent(SigmaFormation12, …)`** while temporarily forcing **`SkipNonGuideExecuteSigmaFormation12HybridPass`** so post-bond matches the shared σ-12 path without double non-guide refresh. **`SigmaBondFormation`:** after interaction block, **`SnapToOriginal()`** on the tie-break dragged lobe again and **`yield return null`** so placement/prebond read geometry after restore; phase 3 replaces direct **`RefreshSigmaBondOrbitalHybridAlignmentAfterFormationRedistribute`** with **`RunElectronRedistributionForBondEvent`** (guide-only via the bond flag). Debug NDJSON for session `65fd4c` and related **`CovalentBond`** probes were removed in the same workstream (clean tree on those files at HEAD unless amended).

---

_`Ref: pending` in `.cursor/compact-last.md` until this working tree is committed._
