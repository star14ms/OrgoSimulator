# Update summary — 2026-04-23 (pi guide, mutual σ, redistribution)

Since last `/compact` (working tree vs `HEAD`): **ElectronRedistributionGuide** fixes `ResolveGuideAtomForPair` when the pair already shares a σ bond—heavier substituent across that cut maps to the correct guide/non-guide endpoints; whole-molecule mass tie-break is unchanged. **OrbitalRedistribution** adds π-oriented helpers (`TryGetGuideGroupOrbitalForPiPrebondReference`, `BothPiPairGuideGroupsAreMutualInterAtomSigma`), refactors VSEPR list collection for guide-path checks, passes `guideGroup` into `BuildRedistributionAnimation`, tightens `GetHeaviestGroupOpPrioritized` with σ-edge effective masses, adds chain-style bond-row selection / child-recursion gating (including cyclic + OP-pivot conditions), removes the obsolete breaking-empty special-case path, and fixes template alignment `else` bracing. **SigmaBondFormation** implements π phase‑1 as non-guide primary plus optional guide track when mutual σ guide groups match, improves shared π prebond +z from guide-group crosses and fallbacks, adds `TryGetSigmaLineOrbitalBetween`, and updates class docs. **AtomFunction** rejects NaN / near-zero `plusZWorld` when writing π prebond directions. **ElectronOrbitalFunction** indentation cleanup on the π coroutine fallback.
