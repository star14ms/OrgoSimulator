# AtomFunction: empty lobe ideal check, co-bond framework, separated empty slots

Since prior compact (`Ref: pending`): **`EmptyTipAlreadyIdealVsElectronFramework`** centralizes when a 0e lobe can stay put: optional **undirected** separation from `occupiedLobeAxesMustSeparateFrom` (min(θ, 180°−θ) ≥ 36° vs co-bond / current occupied tips), plus opposite-single-axis or ⊥-to-framework rules; supports **framework-only** or **separation-only** when lists are partial. **`AppendOneRepulsionSumEmptySlot`** refactors repulsion-sum empty placement with **`PickEmptyDirOnAxisAvoidingOtherEmpties`**; passes current occupied lobe tips into the ideal helper. **`TryComputeSeparatedEmptySlot`** prefers candidates with **≥20°** separation from other empties when possible, else falls back. Guide-group **`nOcc==0`** builds **`electronFrameworkForEmpty`** from **guideTip + co-bond** tips; **`CollectGuideGroupCoBondOrbitalTipsNucleusLocal`** + **`AppendUniqueFrameworkDirection`**; skip/apply use expanded framework and co-bond axes. **`nOcc>0`** empty loop passes mover tips + co-bond tips as separation list for the same helper.
