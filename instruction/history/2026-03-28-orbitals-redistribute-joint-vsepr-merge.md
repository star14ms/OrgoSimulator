# Orbitals: joint apply, VSEPR merge, π guide, quadruple path

Since prior compact (pending ref): `AtomFunction` 3D redistribute was extended so **ApplyRedistributeTargets** uses one **joint rotation** at the nucleus from paired current/desired hybrid tips (orthonormal-basis helper), then moves all σ partner fragments together, updates bonds, applies nucleus locals, then **ApplySigmaOrbitalTipFromRedistribution** per σ. **Guide-group** layout now **merges** all bond orbitals on the **same atom pair** into one VSEPR group (single through quadruple), drops co-bond rows from targets like the fixed guide, aligns **π** guide frames to **internuclear axis** (not π lobe) for trigonal planar, and handles **nOcc == 0** after merge (sole multiply-bonded neighbor) with empty-only or no targets so repulsion **electronDomains** does not re-aim σ and move the partner. **GetRedistributeTargets3DRepulsionLayoutOnly** accepts **empty** successful guide prefix lists. Added **DebugLogRedistribute3DApplyBondParented** and remarks on repulsion slot space; angle diag text updated for σ hybrid apply.
