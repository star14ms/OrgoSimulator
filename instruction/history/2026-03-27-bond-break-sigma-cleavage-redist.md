# Summary (since last /compact)

Bond break / orbital redistribution work tightens **full σ cleavage** vs **π-only** steps: `RedistributeOrbitals` and `RedistributeOrbitals3D` take `bondBreakIsSigmaCleavageBetweenFormerPartners`, and `GetRedistributeTargets3D` uses it so VSEPR is not driven by σ-cleavage framing when the σ edge still exists. `AtomFunction` adds `IsBondBreakGuideOrbitalWithBondingCleared`, requires a cleared ex-bond guide for carbocation slot logic where documented, refines `pinEmptyUnmoved` for 0e vs lone-pair-on-guide cases, and only runs the extra “0e guide ⊥ framework” placement in `RedistributeOrbitals3D` when there is **no** bond-break ref. `TryComputePerpendicularEmptySlotFromFrameworkDirs` gains a single-framework 180° opposite default and an optional preferred direction. When several local 0e non-bonds could serve as the σ-cleavage guide, the choice is ordered deterministically by `GetInstanceID()` (no per-break stamps on orbitals).

`OrientEmptyNonbondedOrbitalsPerpendicularToFramework` is reworked: clearer branches (sole 0e guide, σ-cleavage pin, multi-empty separation, generic ⊥ framework) with shared finish via `OrientEmptyFinishCarbSnap`; **removed** the former σN==0 linear **2 occupied + 2 empty** carbocation slot builder (`TryComputeSigmaNZeroExBondGuideLinearBondBreakSlots`) and the OrientEmpty loop that paired dual 0e lobes—left a short “pending rewrite” comment. Debug/snapshot logging for empty teleport branches is expanded; carbocation multi-empty helpers align with the new perpendicular helper signature.

`CovalentBond` passes the **full** break guide into `OrientEmpty` (including 0e) so skip/pin behavior matches the real ex-bond lobe; builds explicit **σ-cleavage pinned empty** sets for animation; `BondBreakGuideLoneOrbitalOnAtom` resolves the guide from parent/topology and 0e preference with instance-id tie-breaks. `CoAnimateBreakBondRedistribution` keeps frozen preview authoritative (occupied motion when σ-relax has no nuclear moves), skips bond→slot lerp for full σ cleavage / pinned empties, guards post-anim slot settle against moving fixed guides, and documents why frozen non-bond apply still runs for guides excluded from redist lerp. `.cursor/rules/debug-log-bracket-single-line.mdc` is updated to require a **single leading** `[tag]` for filtering and to forbid extra bracketed fragments in the message body.

`ElectronOrbitalFunction`: in perspective, **keeps** the bond lobe mesh during drag when a σ bond exists so stretch visuals do not hide electrons.
