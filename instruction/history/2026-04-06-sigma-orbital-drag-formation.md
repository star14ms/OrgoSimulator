# 2026-04-06 — σ orbital-drag formation and redistribution wiring

Since last compact (ref was pending; summarized vs `ab81d83` plus working tree): Added **`SigmaBondFormation`** with a three-phase coroutine (pre-bond unified shell + fragment approach in `LateUpdate`, bond create + cylinder + orbital-to-line, optional post-bond guide hybrid lerp). Introduced **`ElectronRedistributionGuide`** (guide/non-guide by molecular mass + drag tie-break) and **`ElectronRedistributionOrchestrator`** (pre-bond rigid shell align, post-bond guide refresh, `SigmaFormation12` hybrid passes with **`SkipNonGuideExecuteSigmaFormation12HybridPass`** to avoid double TryMatch after phase 1).

Extended **`AtomFunction`** with **`CollectSigmaPrebondRigidShellOrbitals`** (nucleus list ∪ bond GO orbitals ∪ forming op), matching capture/snapshot helpers, and rigid rotation for that shell. **`CovalentBond`** gained **`suppressSigmaPrebondBondFrameOrbitalPose`** and snap/pose guards; removed obsolete step-2 peripheral rot freeze in favor of suppression.

**`EditModeManager`** now exposes **`FormSigmaBondInstantBody`**, phase-3 **`CoLerpSigmaFormationNucleusOrbitalLocals`**, and **`FinishSigmaBondInstantTail`** (with skip for H σ-neighbor snap after orbital-drag three-phase). **`ElectronOrbitalFunction`** consolidates σ-drag entry to the runner, splits phase 2 into explicit cylinder / orbital-to-line seconds (no legacy total × ratio), and keeps cylinder **skip** when pos + lobe direction already match targets. **`MoleculeBuilder`** calls the orchestrator when redistribute flags ask for it. Prefabs updated for the new timing fields.

Docs: new **`instruction/sigma-bond-formation-orbital-drag-reimplementation.md`** (pitfalls checklist); updates to redistribution / chemical-reaction instruction and cursor rules. Minor **`BondFormationDebugHud`** fix (avoid duplicate `RectTransform`). Removed triage-only logging (`DebugLogPrebondLateUpdate`-style probe, orchestrator event logs, prebond-rigid skip log).
