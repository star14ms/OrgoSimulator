Since the last compact, the working tree adds a debug-first pass and targeted fixes across functional-group attach, benzene hydrogen placement, and input shortcuts. `EditModeManager.cs` and `MoleculeBuilder.cs` now include session-scoped NDJSON instrumentation for FG attach and benzene saturation diagnostics, plus production fixes that stop fallback FG π promotion on the substrate attachment root and keep π-center hydrogen placement on VSEPR-derived directions. `AtomQuickAddUI.cs` adds a `P` key shortcut that spawns phosphorus (`atomicNumber=15`) through the same element-click path as other hotkeys. In parallel, ring/π geometry work continued in `SigmaBondFormation.cs` (new cyclic coplanar phase-1 track) and `OrbitalRedistribution.cs` (ongoing redistribution adjustments and debug output).
