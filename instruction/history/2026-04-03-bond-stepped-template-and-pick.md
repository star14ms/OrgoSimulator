# Bond stepped-debug template, picking, and phase UX

## Summary (since last /compact)

Uncommitted work on bond stepped-debug **green template** preview and interaction: **`BondFormationTemplatePreviewInput`** now runs in **LateUpdate** (with execution order vs **`BondFormationTemplateStemRayPick`**), uses **trigger-aware** physics raycasts, **mouse deviceId** / **UI raycast** so only **interactable** `Selectable` HUD controls block world picks (not full-screen panels), widens **stem** pick radius, and syncs stem **segment** endpoints each frame. **`BondFormationTemplateDescriptionUI`** adds a **fallback overlay Canvas** when none exists, raises **sorting** via nested Canvas, disables **Image** raycast targets, and **increases** description font/panel size. **`CovalentBond`** ignores pointer down/drag/up while **`BondFormationDebugController.IsWaitingForPhase`** so real bond cylinders are not used during phase waits. **`ElectronOrbitalFunction`** removes the **lerped** phase-to-phase template transition in favor of **`SwapSteppedTemplatePreviewRootInstant`**. **`ProjectAgentDebugLog`** adds **`AppendDebugModeNdjson`** for session files under `.cursor/`. **`BondFormationTemplatePreviewInput`** still contains **folded agent-debug NDJSON** logging to `debug-9ddc95.log` (remove or gate before shipping if desired).
