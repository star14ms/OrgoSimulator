# 2026-04-21 — π perspective visuals, OP formation, bond drag

Uncommitted work since last marker: **CovalentBond** gains perspective π geometry (dual cylinders, ± lobe visuals, colliders/slop for −Z), cached π axis from phase-1 / neighbors, bond-line **π drag** that spawns the shared orbital at the pointer (no snap to +Z center only), and pointer-down screen capture for correct drag offset. **ElectronOrbitalFunction** updates π cylinder prep (per-atom +Z targets), **OP phase-2 motion** that sweeps orbital centers on **nucleus pivot arcs** (slerp direction + lerp radius) instead of linear chords through atoms, plus related formation/debug touches. **SigmaBondFormation** / **OrbitalRedistribution** / **EditModeManager** carry the orbital-drag π pipeline and redistribution hooks. **AtomFunction** adds carbocation σ-break trigonal template helper from σ-tip plane / cleavage fallback. **AminoAcidMoleculeLibrary** extends side-chain construction (His imidazole path, ring closures via **TryBondDirect** using instant σ/π formation runners). Instruction doc for σ–π orbital-drag reimplementation nudged.
