# 2026-03-23 — Bond break, C–C attach, methyl H-auto, debug logging

Since last compact (`ade48a0` / pending marker): large 3D edit-mode and redistribution pass. **CovalentBond** instant break for edit replace-H skips VSEPR preview and heavy-atom σ churn when the partner is hydrogen (avoids spurious orbital “rotation” on the anchor after C–H break). **AtomFunction** adds carbocation vs radical handling for sp² bond-break σ layout, tet σ-relax for 3→4 σ with no occupied lone lobes (formation + animation hints), σ-count gating for redistribute targets, **SnapHydrogenSigmaNeighborsToBondOrbitalAxes** (geometric C→H), **TryPlaceTetrahedralHydrogenSubstituentsAboutSingleHeavyNeighbor** (final —CH₃ placement vs one heavy neighbor after H-auto), and tet σ-relax alignment to the **heavy** neighbor when bonding to H with a single C neighbor. **EditModeManager** wires redistributeAtomA/B for attach, instant break for eraser/replace-H, H-auto logging (`[attach-added-group]`), **SaturateWithHydrogen** overload for per-H logs, and post-saturate methyl fixup. **ElectronOrbitalFunction** animates tet σ-relax on σ formation where applicable. **ProjectAgentDebugLog** mirrors tagged lines to `.log`. Raycaster preference scripts and minor UI/builder touch-ups. User-confirmed: CH₃–CH₃ break/bond and general C–C attach/H-auto issues resolved.
