# Update summary (since last /compact)

**Scope:** Uncommitted work on `debug-orbital-redistribution` vs `HEAD` (`a664c0f…`).

**σ bond formation (3D)** — `ElectronOrbitalFunction` / `CovalentBond`: animated step-2 now avoids double-applying σ orbital poses by calling `UpdateSigmaBondLineTransformsOnlyForAtoms` inside the step-2 loop (bond frame only; full `UpdateSigmaBondVisualsForAtoms` remains after peripheral freeze ends). When post-formation redistribute/hybrid apply is deferred (`skipPostFormationRedistAndHybrid3D`), bonding σ world rotation stays fixed at step-2 start. Peripheral σ lines use world-rotation freeze during pure σ-relax; `CommitSigmaRedistributionDeltaFromWorldOrbitalRotation`, optional `SnapOrbitalToBondPosition` roll preservation, `UpdateSigmaBondVisualsAfterBondTransform`, and `ApplySigmaOrbitalTipFromRedistribution` anti-parallel handling reduce gauge pops. Heavy diagnostics: `[σ-form-rot-precalc|pose|endApply|bond]` with optional `dRotOrbVsLockedStartDeg` when comparing to locked start rotation.

**Bond break / redistribute triage** — `AtomFunction` / `CovalentBond`: new flags and one-line logs (`[break-tetra]`, `[break-motion]`, `[break-σ-relax]`, `[break-redistribute]`), `LogBondBreakTetraFrameworkSnapshot`, hydrogen σ-snap summary when `DebugLogHydrogenSigmaSnap`, richer `TryComputeSp2` / rigid-cleavage logging. Tetrahedral σ-relax target mapping tries four vertex labelings to minimize unpinned motion; skip rigid/sp² relax when framework already ~tetrahedral. **`RedistributeOrbitals3D` is temporarily a NO-OP** (call to `RedistributeOrbitals3DOld` commented) with stub logging — intentional for testing per workspace rule on the forwarder body.

**Repo / rules** — Added `.cursor/rules/debug-log-bracket-single-line.mdc` (bracket prefix, one line per log, no `\n`). Added `instruction/history/2026-03-25-sigma-formation-rotation-bugs-and-fixes.md`; removed `2026-03-25-bond-break-skip-sp2-rigid-spin.md`. `TODO` and `.cursor/compact-last.md` touched.

---

*Generated by `/compact` on 2026-03-25.*
