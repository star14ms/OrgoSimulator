Updated the orbital-drag π pipeline to run through the `SigmaBondFormation` three-phase flow, including phase-1 redistribution plus per-frame cylinder updates that track moving centers. Refactored shared cylinder prep/finalize APIs in `ElectronOrbitalFunction` and `CovalentBond` so σ/π lines can target explicit bond-line indices and keep bond visuals synchronized during animation.

Extended `OrbitalRedistribution` with sigma-only group counting, guide-template alignment helpers, trigonal π eclipsed alignment, and operation-path-aware chain blocking so the op axis does not trigger unwanted recursive redistribution. Also aligned chain motion behavior by rotating π groups with their paired σ rigid rotation and cleaned stale guards/TODO notes related to resolved redistribution issues.
