# Summary (since last /compact)

**AtomFunction:** σ-relax for bond formation now merges caller `pinAtomsForSigmaRelax` into tet three/four σ paths; `MaybeApply` and `RedistributeOrbitals3D` pass pins through trigonal-planar and linear relax; trigonal compute API takes optional `pinWorld`. Newman stagger gains a heteroatom / lone-pair fallback (bottleneck vs parent substituents) so replace-H (e.g. O on methyl) can twist. Added opt-in **`[fw-pin]`** trace (`DebugLogFrameworkPinSigmaRelaxTrace`, helpers, logs on redistribute entry, tet relax apply/skip, `BuildSigmaRigid`, coplanar/trigonal/linear relax, H snap, Newman twist, `TryPlaceTetrahedralH`).

**EditModeManager:** `FormSigmaBondInstant` supports pins on **both** atom ends (fixes H-auto moving framework when only H got pins). Framework pins use **`GetAtomsOnSideOfSigmaBond`** (full side, not just parent); `TryBuildSigmaRelaxPinForHeavyCenter` for saturate / `AddHydrogenAtDirection`. Toolbar add: stagger before H-auto; replace-H: same stagger order + pin set; clipboard DFS order (`BuildOrderedMoleculeAtomList`, H-before-heavy, root from selection). `SaturateAtomsWithHydrogenPass` accepts optional pin; FG replace calls `BuildFunctionalGroup` with parent geometry preserved.

**MoleculeBuilder:** Functional groups: initial pin = full `parent` connected molecule before first σ bond; after attachment refresh pin via `anchor.GetAtomsOnSideOfSigmaBond(parent)`; all internal `BondSigma` pass pins; `FormSigmaBondInstant` passes pins into `RedistributeOrbitals`; post-π redistribute uses except-parent + pins; Newman stagger on FG backbone heavies (BFS toward parent) **before** H saturation; saturation pass forwards pin to edit mode.
